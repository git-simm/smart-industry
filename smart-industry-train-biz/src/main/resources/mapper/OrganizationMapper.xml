<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="smart.industry.train.biz.mapper2.OrganizationMapper">
    <select id="getOrgList" resultType="smart.industry.train.biz.entity.Organization">
        SELECT a.*,b.name as pName FROM organization as a
        left join organization as b on a.pId = b.id
        <where>
            ${ew.sqlSegment}
        </where>
        order by a.pId,a.sort
    </select>
</mapper>